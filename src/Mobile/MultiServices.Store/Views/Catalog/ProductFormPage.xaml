<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:vm="clr-namespace:MultiServices.Store.ViewModels"
       x:Class="MultiServices.Store.Views.Catalog.ProductFormPage" x:DataType="vm:ProductFormViewModel" Title="{Binding Title}">
    <ScrollView><VerticalStackLayout Spacing="16" Padding="16">
        <Frame Style="{StaticResource Card}"><VerticalStackLayout Spacing="12">
            <Label Text="Informations produit" Style="{StaticResource H3}" />
            <Entry Placeholder="Nom *" Text="{Binding Name}" /><Editor Placeholder="Description" Text="{Binding Description}" HeightRequest="60" />
            <Entry Placeholder="Marque" Text="{Binding Brand}" />
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8"><Entry Placeholder="Code-barre" Text="{Binding Barcode}" /><Button Grid.Column="1" Text="📷 Scan" Command="{Binding ScanBarcodeCommand}" BackgroundColor="{StaticResource Info}" TextColor="White" CornerRadius="8" HeightRequest="40" /></Grid>
        </VerticalStackLayout></Frame>
        <Frame Style="{StaticResource Card}"><VerticalStackLayout Spacing="12">
            <Label Text="Prix & Stock" Style="{StaticResource H3}" />
            <Entry Placeholder="Prix (MAD) *" Text="{Binding Price}" Keyboard="Numeric" />
            <Entry Placeholder="Prix au kg/L" Text="{Binding PricePerUnit}" Keyboard="Numeric" />
            <Entry Placeholder="Unité (kg, L, pièce, pack)" Text="{Binding UnitMeasure}" />
            <Entry Placeholder="Stock *" Text="{Binding Stock}" Keyboard="Numeric" />
        </VerticalStackLayout></Frame>
        <Frame Style="{StaticResource Card}"><VerticalStackLayout Spacing="12">
            <Label Text="Détails produit" Style="{StaticResource H3}" />
            <Entry Placeholder="Infos nutritionnelles" Text="{Binding NutritionalInfo}" /><Entry Placeholder="Allergènes" Text="{Binding Allergens}" />
            <Entry Placeholder="Origine" Text="{Binding Origin}" />
            <HorizontalStackLayout Spacing="16"><Label Text="Bio" VerticalOptions="Center" /><Switch IsToggled="{Binding IsBio}" /><Label Text="Halal" VerticalOptions="Center" /><Switch IsToggled="{Binding IsHalal}" /></HorizontalStackLayout>
        </VerticalStackLayout></Frame>
        <Button Text="📷 Photo produit" Command="{Binding UploadPhotoCommand}" BackgroundColor="White" TextColor="{StaticResource Primary}" BorderColor="{StaticResource Border}" BorderWidth="1" CornerRadius="8" HeightRequest="44" />
        <Button Text="Enregistrer" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryBtn}" />
    </VerticalStackLayout></ScrollView>
</ContentPage>