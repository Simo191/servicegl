<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:vm="clr-namespace:MultiServices.Store.ViewModels"
       x:Class="MultiServices.Store.Views.Auth.LoginPage" x:DataType="vm:LoginViewModel" Title="{Binding Title}">
    <ScrollView><VerticalStackLayout Spacing="20" Padding="32" VerticalOptions="Center">
        <Label Text="🛒" FontSize="60" HorizontalOptions="Center" />
        <Label Text="Espace Magasin" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource Primary}" />
        <Frame Style="{StaticResource Card}" Padding="20"><VerticalStackLayout Spacing="16">
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" /><Entry Placeholder="Mot de passe" Text="{Binding Password}" IsPassword="True" />
            <Button Text="Se connecter" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryBtn}" />
            <Label Text="{Binding ErrorMessage}" TextColor="{StaticResource Danger}" IsVisible="{Binding HasError}" HorizontalOptions="Center" />
        </VerticalStackLayout></Frame></VerticalStackLayout></ScrollView>
</ContentPage>