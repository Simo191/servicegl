<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MultiServices.Deliverer.ViewModels"
             x:Class="MultiServices.Deliverer.Views.EmergencyContactPage"
             x:DataType="vm:EmergencyContactViewModel"
             Title="Contact d'urgence"
             BackgroundColor="{StaticResource Background}">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            <Frame Style="{StaticResource CardFrame}" BackgroundColor="#FFF3E0">
                <HorizontalStackLayout Spacing="10">
                    <Label Text="🆘" FontSize="28" VerticalOptions="Center" />
                    <VerticalStackLayout VerticalOptions="Center">
                        <Label Text="Contact d'urgence" FontSize="16" FontAttributes="Bold" />
                        <Label Text="Cette personne sera contactée en cas d'urgence via le bouton SOS"
                               FontSize="13" TextColor="{StaticResource TextSecondary}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Frame>

            <Label Text="Nom complet *" FontSize="14" FontAttributes="Bold" />
            <Entry Placeholder="Nom et prénom" Text="{Binding ContactName}" Style="{StaticResource FormEntry}" />

            <Label Text="Téléphone *" FontSize="14" FontAttributes="Bold" />
            <Entry Placeholder="+212 6XX XXX XXX" Text="{Binding ContactPhone}" Keyboard="Telephone" Style="{StaticResource FormEntry}" />

            <Label Text="Relation" FontSize="14" FontAttributes="Bold" />
            <Entry Placeholder="Ex: Père, Mère, Conjoint(e)..." Text="{Binding Relationship}" Style="{StaticResource FormEntry}" />

            <Button Text="Sauvegarder" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" Margin="0,10,0,0" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Primary}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
