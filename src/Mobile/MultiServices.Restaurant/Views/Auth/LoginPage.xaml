<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:MultiServices.Restaurant.ViewModels"
             x:Class="MultiServices.Restaurant.Views.Auth.LoginPage" x:DataType="vm:LoginViewModel" Shell.NavBarIsVisible="False" BackgroundColor="{StaticResource Background}">
    <ScrollView><VerticalStackLayout Spacing="20" Padding="32" VerticalOptions="Center">
        <Image Source="restaurant_logo.png" HeightRequest="80" WidthRequest="80" HorizontalOptions="Center" />
        <Label Text="Espace Restaurant" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource Primary}" />
        <Label Text="Gérez vos commandes, votre menu et vos statistiques" FontSize="13" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" HorizontalTextAlignment="Center" />
        <Frame Style="{StaticResource Card}" Padding="20"><VerticalStackLayout Spacing="16">
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Mot de passe" Text="{Binding Password}" IsPassword="True" />
            <Button Text="Se connecter" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryBtn}" />
            <Label Text="{Binding ErrorMessage}" TextColor="{StaticResource Danger}" IsVisible="{Binding HasError}" HorizontalOptions="Center" FontSize="13" />
        </VerticalStackLayout></Frame>
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Primary}" />
    </VerticalStackLayout></ScrollView>
</ContentPage>